server.port=8082

## 多注册中心，高可用

### 这里的 localhost 应该为 注册中心 1 的 ip，因为我现在同一台机器下测试，所以使用了 localhost
eureka.instance.hostname=localhost

eureka.client.service-url.defaultZone=http://${eureka.instance.hostname}:8081/eureka/